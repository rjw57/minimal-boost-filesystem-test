Minimal Boost filesystem library test
=====================================

Compile with CMake:

    $ git clone git@github.com:rjw57/minimal-boost-filesystem-test.git
    $ cd minimal-boost-filesystem-test/
    $ mkdir build/
    $ cd build/
    $ cmake ..
    $ make && ./boost_test

